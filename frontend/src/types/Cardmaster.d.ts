type map = {
  [key: string]: number
}

interface Game {
  id: string
  points: object
  fines: object
}

interface GameSession {
  id: string
  players: Object
}

interface User {
  id: string
}

interface Group {
  id: string
  players: Object

}

interface PlaysIn {
  id: string

}
